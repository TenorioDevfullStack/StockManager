// Copie este arquivo para js/supabase.config.js e preencha os valores reais.
// Este projeto roda direto no navegador, então arquivos .env não são carregados
// automaticamente sem uma etapa de build.

window.SUPABASE_CONFIG = {
  url: 'https://seu-project-ref.supabase.co',
  anonKey: 'sua-chave-anon-public',
  allowSignUp: false,
};
